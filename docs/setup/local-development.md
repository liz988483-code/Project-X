# Local Development Setup

## Prerequisites
- Node.js 18+
- Docker and Docker Compose
- PostgreSQL (optional - Docker will run it)
- Redis (optional - Docker will run it)

## Setup Steps

1. Clone the repository:
```bash
git clone <repo-url>
cd soko